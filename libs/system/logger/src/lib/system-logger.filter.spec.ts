import { SystemLoggerFilter } from './system-logger.filter';

describe('SystemLoggerFilter', () => {
    it('should be defined', () => {
        expect(new SystemLoggerFilter()).toBeDefined();
    });
});
