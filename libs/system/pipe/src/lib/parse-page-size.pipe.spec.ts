import { ParsePageSizePipe } from './parse-page-size.pipe';

describe('ParsePageSizePipe', () => {
    it('should be defined', () => {
        expect(new ParsePageSizePipe()).toBeDefined();
    });
});
